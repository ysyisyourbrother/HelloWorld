## 云端启动
在服务器上（SSH 进去后）
```bash
python launch_cloud_system.py
```
保持 configs/config.json 里云端 API 不变："host": "127.0.0.1", "port": 9000 即可。
## 本地配置
在本地新建一个SSH隧道：
```bash
ssh -L 9000:127.0.0.1:9000 531_n4
```
保持这个窗口。然后在本地另开一个终端启动：
```bash
python launch_client_onlyText.py # 只有文本交互
python launch_client_withImage.py # 图像交互
```
```bash
python launch_edge_system.py
```